apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app
  namespace: default
  labels:
    app: todo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      containers:
        - name: todo-app
          image: imarcho/myapp:ed84988d34ca1bff24ed341630e0dac3b5ffe7d7
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: SQLITE_DB_LOCATION
              value: "/data/todos.db"
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          emptyDir: {}
